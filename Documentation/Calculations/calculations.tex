\documentclass{letter}
\usepackage{graphicx}
\usepackage{anyfontsize}
\usepackage[nocheck]{fancyhdr}
\usepackage{Tabbing}
\usepackage{soul}
\usepackage{color}
\usepackage[a4paper,
            bindingoffset=0.2in,
            left=1in,
            right=1in,
            top=1in,
            bottom=1in,
            footskip=.25in]{geometry}

\pagestyle{fancy}
\begin{document}
\fancyhead[C]{ \fontsize{20pt}{20pt}\selectfont Table of Contents }
\fancyhead[R]{\fontsize{15pt}{15pt}\selectfont \thepage}
\fancyfoot{}

\begin{flushleft}
    \fontsize{15pt}{15pt}\selectfont

    \textbf{Derivations of RC LPF and HPF Transfer Functions} \hfill pages 2 - 4

    \end{flushleft}

\pagebreak

\fancyhead[C]{}



\begin{center}
\vspace{4.5in} 
\fontsize{30pt}{30pt}\selectfont
\textbf{Derivations of RC LPF and HPF Transfer Functions}
\end{center}
\vfill

\pagebreak

\fancyhead[C]{ \renewcommand{\plainheadrulewidth}{0px} \fontsize{20pt}{20pt}\selectfont Derivations }

\begin{flushleft}
    \setlength{\parindent}{30pt}
    \fontsize{12pt}{12pt}\selectfont

    The following section will be used as an exercise deriving the transfer functions of second order
    RC Low Pass and High Pass filters. The filters that make up the graphical EQ will be a combination of
    cascaded second order high pass and low pass filters coupled using operational amplifiers as buffers to
    prevent the first filter from loading the second filter. A filter is a circuit that allows spectral components
    of a signal within a certain bandwidth to pass to its output while attenuating other components of the 
    signal outside of the bandwidth. If a signal is thought of as a mixture, a filter can be thought of as
    a means by wich to separate desirable components and discard undesirable portions of a signal.
     An RC filter uses resistors and capacitors to achieve this separation.

    The first filter we will look at is the second order low pass filter formed by cascading 2 RC low pass filters:

    
        \begin{center} \includegraphics{LPF.png}  
            
            \textbf{Figure 1.} Second Order RC Lowpass Filter \end{center} 

            \vspace{.5in}

    The above circuit, figure 1, is the schematic for the second order low pass filter. The Capacitors act as a means to short high frequency signals/signal components to ground
    while allowing low frequency components to pass to the output. Note that there are 3 nodes of interest in this circuit: The input $V_{in}$, $V_{1}$, and the output $V_{out}$.
    These nodes will be of direct interest for our derivation of the transfer function of this circuit. The resistors $R_{1}$ and $R_{2}$, as well as, the capacitors
    $C_{1}$ and $C_{2}$ are used to tune the bandwidth and the damping/quality factor of the circuit.

            
\end{flushleft}
 
\end{document}